import { Oval, TailSpin } from 'react-loader-spinner'

function SightLoader() {
  return (
      <TailSpin
        height="50"
        width="50"
        color="#fff"
        ariaLabel="tail-spin-loading"
      />
  )
}

export default SightLoader